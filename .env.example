# Use same-origin /api (recommended).
# - Local: Vite proxies /api → staging backend
# - Vercel: vercel.json rewrites /api → staging backend
# This avoids browser CORS because the page never calls Railway directly.
VITE_API_BASE_URL=/api
